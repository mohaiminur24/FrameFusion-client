import React from 'react';
import ContainerLayout from '../../reusedComponents/ContainerLayout';
import SliderLayout from './Layout/SliderLayout';

const HomePage = () => {
    return (
        <div>
            <ContainerLayout><SliderLayout/></ContainerLayout>
            
        </div>
    );
};

export default HomePage;